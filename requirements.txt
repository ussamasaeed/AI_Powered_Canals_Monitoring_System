fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9

# Real external database drivers - only needed if you connect to a
# PostgreSQL or MySQL server from the "Connect Database" screen.
# SQLite (the app's default/local database) needs neither.
psycopg2-binary>=2.9.9
mysql-connector-python>=8.3.0

# RAG chatbot: sentence-transformers embedding model + Chroma vector store,
# python-dotenv to load config from .env.
python-dotenv>=1.0.0
chromadb>=0.5.0
sentence-transformers>=3.0.0

# Chat model backend - two options, pick via LLM_PROVIDER in .env:
#   LLM_PROVIDER=ollama (default) - talks to a local Ollama server over
#     plain HTTP (stdlib urllib), so no extra package is needed here.
#     Install Ollama itself separately from https://ollama.com/download
#   LLM_PROVIDER=hf - calls the Hugging Face Inference API, needs this:
huggingface_hub>=0.27.0
