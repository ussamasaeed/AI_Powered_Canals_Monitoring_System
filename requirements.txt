fastapi>=0.110.0
uvicorn[standard]>=0.29.0
jinja2>=3.1.0
python-multipart>=0.0.9

# Real external database drivers - only needed if you connect to a
# PostgreSQL or MySQL server from the "Connect Database" screen.
# SQLite (the app's default/local database) needs neither.
psycopg2-binary>=2.9.9
mysql-connector-python>=8.3.0

# RAG chatbot (update 1): Hugging Face open-source embedding + chat model,
# Chroma as the vector store, python-dotenv to load HF_API_TOKEN from .env
python-dotenv>=1.0.0
chromadb>=0.5.0
sentence-transformers>=3.0.0
huggingface_hub>=0.27.0
